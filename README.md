## Database Setup

To run this application locally, you must initialize the database using Microsoft SQL Server:

1. Open **SQL Server Management Studio (SSMS)**.
2. Open and execute the `db_setup.sql` script included in this repository root.
3. Update the connection string in the presentation layer's configuration file to match your local server instance name.
